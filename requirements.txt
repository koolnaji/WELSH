pandas>=2.2,<3
requests>=2.31,<3
tqdm>=4.66,<5
# yt-dlp>=2025.1.0 lower bound kept as a floor. The 2026-08 403 storm on
# low-view videos (see limitations.txt Section 1.4) is resolved
# operationally, not in code: switch to a nightly build when a storm
# recurs, revert to a pinned stable release once it clears. If you
# adopt nightly, record the exact build string here as a comment
# (`yt-dlp --version`) since nightly is not semver-stable and a later
# build could silently change this behavior again.
yt-dlp>=2025.1.0
faster-whisper>=1.1,<2
torch>=2.2,<3
spacy>=3.8,<4
simplemma>=1.1,<2
matplotlib>=3.8,<4
seaborn>=0.13,<1